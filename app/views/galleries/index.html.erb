<%= render 'pages/header' %>

<p><%= link_to 'Create new Gallery', new_gallery_path %></p>

<% @galleries.each do |gallery| %>
  <p><%= gallery.title %> </p>
  <p><%= gallery.description %> </p>
  <%= image_tag gallery.thumb_image %>
  <p><%= link_to 'Edit', edit_gallery_path(gallery.id) %></p>
<% end %>