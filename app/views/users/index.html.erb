<h2>Users</h2>
<% @users.each do |user| %>
  <%= link_to user.name, user %></h3>
  <%= user.admin? %></h3>
  <%= link_to 'Edit', edit_user_path(user) %>
<% end %>
<%= link_to 'New User', new_user_path %>
